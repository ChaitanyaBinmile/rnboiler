import { combineReducers } from 'redux';
import reducerScreenHome from '../../screens/ScredenExample/redux/reducerScreeHome';

const rootReducer = combineReducers({
  reducerScreenHome,
});
export default rootReducer;
